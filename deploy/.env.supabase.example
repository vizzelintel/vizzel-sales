# Supabase source for one-time migrate (chmod 600). Never commit.
# Supabase Dashboard → Project Settings → Database → Connection string → URI (Session pooler or Direct)
SUPABASE_DB_URL=
# Optional — legacy document download from Supabase Storage until re-upload:
# SUPABASE_URL=https://YOUR_REF.supabase.co
# SUPABASE_SERVICE_KEY=
